[build-system]
requires = ["setuptools>=77,<83"]
build-backend = "setuptools.build_meta"

[project]
name = "second-paddle-process-receipt"
version = "0.2.0a1"
description = "Run a trusted command and record observed direct-process exit evidence."
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
license-files = ["LICENSE"]
authors = [{name = "Youngseok Oh"}]
keywords = ["process", "supervision", "receipt"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Environment :: Console",
    "Operating System :: POSIX",
    "Operating System :: Microsoft :: Windows",
    "Programming Language :: Python :: 3 :: Only",
]
dependencies = []

[project.scripts]
process-receipt = "process_receipt_cli:main"

[project.urls]
Source = "https://github.com/YS-OH-CORE/second-paddle-notes/tree/main/tools/process-receipt"
Issues = "https://github.com/YS-OH-CORE/second-paddle-notes/issues"

[tool.setuptools]
py-modules = ["process_receipt", "process_receipt_cli", "process_receipt_windows"]
